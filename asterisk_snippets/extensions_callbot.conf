; Add to extensions.conf: #include extensions_callbot.conf
; Then: asterisk -rx "dialplan reload"

[from-internal-app]
; From mobile softphone, dial 7001 to reach the Python ARI app (STASIS_APP=callbot).
exten => 7001,1,NoOp(Voice LLM via ARI)
 same => n,Stasis(callbot)
