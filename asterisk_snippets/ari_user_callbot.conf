; EXAMPLE ONLY — change password= before production; must match .env ARI_PASSWORD.
; Add to /etc/asterisk/ari.conf under [general] ensure enabled=yes, then append:
;
; #include ari_user_callbot.conf
;
; Or paste the [callbot] stanza. Reload: asterisk -rx "module reload res_ari.so"

[callbot]
type=user
read_only=no
password_format=plain
password=1001
